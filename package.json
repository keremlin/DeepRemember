{
  "name": "aitrainer",
  "version": "0.0.1",
  "description": "AI Trainer - Complete application with separated frontend and backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["ai", "subtitle", "learning", "express", "frontend", "backend"],
  "author": "Arash Yeganehrad",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {},
  "devDependencies": {}
}
